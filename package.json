{
  "name": "gotcha",
  "version": "0.0.1",
  "description": "psych but in the browser",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js & (cd static && npx node-sass --watch -q . -o .)"
  },
  "author": "Adrian David",
  "license": "MIT",
  "dependencies": {
    "bootstrap-css-only": "^4.4.1",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^14.6.4",
    "@types/socket.io": "^2.1.11",
    "node-sass": "^4.14.1",
    "nodemon": "^2.0.4",
    "typescript": "^4.0.2"
  }
}

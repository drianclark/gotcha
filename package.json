{
  "name": "gotcha",
  "version": "0.0.1",
  "description": "psych but in the browser",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "concurrently \"nodemon server/server.js\" \"npx tsc\"",
    "test": "jest",
    "build": "cd react-ui && npm install && npm run build"
  },
  "author": "Adrian David",
  "license": "MIT",
  "dependencies": {
    "bootstrap-css-only": "^4.4.1",
    "express": "^4.17.1",
    "node-fetch": "^3.1.1",
    "socket.io": "^2.3.0",
    "sqlite": "^4.0.14",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.11.1",
    "@types/socket.io": "^2.1.11",
    "concurrently": "^5.3.0",
    "node-sass": "^4.14.1",
    "nodemon": "^2.0.4",
    "typescript": "^4.0.2"
  }
}
